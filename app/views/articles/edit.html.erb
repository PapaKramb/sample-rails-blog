<h1><%= t('articles.edit_articles') %></h1>

<%= form_for :article, url: article_path(@articles), method: :put do |f| %>
	<p>
		<%= f.label :title %> </br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :text %> </br>
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>
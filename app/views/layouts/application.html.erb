<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="/assets/application.js" type="text/javascript"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link data-require="bootstrap@*" data-semver="4.5.2" rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <script data-require="bootstrap@*" data-semver="4.5.2" src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
          <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <%= link_to 'Blog on Rails', home_index_path, class: "navbar-brand" %>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="nav navbar-nav">
                <li><%= link_to 'Home', home_index_path %></li>
                <li><%= link_to 'Article', articles_path %></li>
                <li><%= link_to 'New article', new_article_path %></li>
                <li><%= link_to 'Contact', contacts_path %></li>
              <div class="nav navbar-nav" style="left: 600px;">
              <% if user_signed_in? %>
                  <li><%= link_to 'Hello,' + "#{current_user.username}" %></li> 
                  <li><%= link_to 'Sign out!', destroy_user_session_path, method: :delete %></li>

                <% else %>

                    <li><%= link_to 'Sign in', new_user_session_path %></li>

                <% end %>
              </div>
              </ul>
            </div>
          </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </div>
  </body>
</html>
